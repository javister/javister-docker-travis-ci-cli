#!/bin/bash

set -e

. proxyenv

if [ "$1" == "bash" ]; then
    shift 1
    setuser system bash $@
elif [ "$1" == "root" ]; then
    shift 1
    bash $@
else
    setuser system bash -c "HOME=${USER_HOME} travis $@"
fi
